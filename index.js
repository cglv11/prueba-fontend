/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import UsersApp from './src/UsersApp';

AppRegistry.registerComponent(appName, () => UsersApp);
